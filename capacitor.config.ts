import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learningmicroacademy.app',
  appName: 'Learning Micro-Academy',
  webDir: 'dist'
};

export default config;
